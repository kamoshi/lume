-- lume no_prelude
-- Auto-imported into every module. Re-exports the core
-- type class hierarchy and polymorphic functions.

use { id, (?>) } = "lume:core"
use ops = "lume:ops"

pub {
  id,
  (?>)
}
